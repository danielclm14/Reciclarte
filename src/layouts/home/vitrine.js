import React, { Component }  from 'react';

export default class bolo extends React.Component {

    render() {
        var style = {
            display:'flex',
            'flex-direction': 'column',
            backgroundColor: 'blue',
        };

        return (<button/>);
    }
}